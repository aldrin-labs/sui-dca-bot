// Move bytecode v6
module flowx_amm::swap_utils {
	use flowx_amm::comparator;
	use std::ascii;
	use std::type_name;
	use sui::coin::Coin;
	
	public fun get_amount_out(arg_0: u64, arg_1: u64, arg_2: u64, arg_3: u64): u64 {
		abort(0)
	}
	
	public fun get_amount_in(arg_0: u64, arg_1: u64, arg_2: u64, arg_3: u64): u64 {
		abort(0)
	}
	
	public fun quote(arg_0: u64, arg_1: u64, arg_2: u64): u64 {
		abort(0)
	}
	
	public fun is_ordered<Ty0, Ty1>(): bool {
		abort(0)
	}
	
	public fun left_amount<Ty0>(arg_0: &Coin<Ty0>, arg_1: u64): u64 {
		abort(0)
	}
}